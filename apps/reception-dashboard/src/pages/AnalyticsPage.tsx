import { PagePlaceholder } from "./PagePlaceholder";

export default function AnalyticsPage() {
  return (
    <PagePlaceholder
      navId="analytics"
      title="Analytics"
      description="Future scope — Phase 6, Prompt 15."
    />
  );
}
