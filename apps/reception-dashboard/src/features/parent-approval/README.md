# parent-approval

Reserved for Reception-side parent-approval session monitoring (Phase 3, Prompt 7B). Must never implement parent authentication, device trust, biometric verification, or the approval decision itself — those are owned by the Parent Application (ADR-001). See `src/services/parent-approval/ParentApprovalService.ts`.
