import { Text, View } from "react-native";

// Placeholder entry screen — framework scaffolding only (Phase 9).
// No product screens (auth, leave status, library pass) exist yet;
// see docs/target-architecture.md for what this app will become.
export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>DigiHostel Student — scaffold, not yet implemented</Text>
    </View>
  );
}
