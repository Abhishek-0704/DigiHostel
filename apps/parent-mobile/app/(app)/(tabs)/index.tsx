import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function Dashboard() {
  return (
    <PlaceholderScreen
      title="Dashboard"
      description="Pending leave approvals for every linked student (GET /leave-requests, G-05) — not implemented in this foundation pass."
    />
  );
}
