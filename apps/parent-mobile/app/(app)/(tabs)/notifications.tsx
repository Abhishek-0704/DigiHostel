import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function Notifications() {
  return (
    <PlaceholderScreen
      title="Notifications"
      description="Push permission, token registration, and notification history — not implemented in this foundation pass."
    />
  );
}
