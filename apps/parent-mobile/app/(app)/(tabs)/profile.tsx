import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function Profile() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="View/edit the authenticated parent's own profile — not implemented in this foundation pass."
    />
  );
}
