import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function Settings() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="App/security settings (theme preference is already live — see Profile/Settings module) — not implemented in this foundation pass."
    />
  );
}
