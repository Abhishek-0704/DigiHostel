import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function Security() {
  return (
    <PlaceholderScreen
      title="Trusted Devices"
      description="List/rename/revoke trusted devices — blocked on a backend endpoint that does not exist yet (see docs/current-state.md's G-04 status)."
    />
  );
}
