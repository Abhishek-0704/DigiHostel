import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function History() {
  return (
    <PlaceholderScreen
      title="Approval History"
      description="Immutable approval-event timeline — backend contract not yet built (RLS exists, no Fastify route)."
    />
  );
}
