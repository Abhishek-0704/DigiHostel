import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function Login() {
  return (
    <PlaceholderScreen
      title="Login"
      description="Roll-number pre-check + Supabase phone-OTP trigger (ADR-020) — not implemented in this foundation pass."
    />
  );
}
