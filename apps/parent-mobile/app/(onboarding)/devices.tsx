import { PlaceholderScreen } from "@/src/components/layout/PlaceholderScreen";

export default function OnboardingDevices() {
  return (
    <PlaceholderScreen
      title="Trust This Device"
      description="Platform attestation + trusted-device registration (ADR-003) — blocked on a backend endpoint that does not exist yet (see docs/current-state.md's G-04 status)."
    />
  );
}
